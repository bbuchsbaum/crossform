% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent.R
\name{latent_geometry}
\alias{latent_geometry}
\title{Construct the latent PSD descriptive layer of a signed geometry}
\usage{
latent_geometry(
  x,
  method = c("psd_projection"),
  component = c("total", "coherent", "configuration"),
  row_block = 1024L
)
}
\arguments{
\item{x}{A complete symmetric effect form from \code{\link[=materialize_geometry]{materialize_geometry()}}, or
the signed \code{effect_spectrum_view} \code{\link[=geometry_spectrum]{geometry_spectrum()}} reads from one.
A query readout has already been contracted against fixed weights and
carries no spectrum, so it is refused.}

\item{method}{The projection to apply, from the latent layer's closed set.
\code{"psd_projection"} truncates the symmetric form's eigenvalues at zero.
\code{"nearest_psd"} is a declared member of the set and is not implemented;
it refuses rather than falling back, because it moves different mass.}

\item{component}{Geometry component to project. Fixed by the view when \code{x}
is an \code{effect_spectrum_view}.}

\item{row_block}{Positive number of measurement rows read per block when
\code{x} is a form.}
}
\value{
An \code{effect_latent_geometry}.
}
\description{
Crossvalidated contributions are \strong{signed}, and the arithmetic that treats
a node's value as part of a nonnegative whole is invalid on them
(\code{design/conservative-geometry-contract.md} section 6): the implied shares
can leave \verb{[0, 1]}, and clipping to nonnegativity both reintroduces the
bias the cross-partition estimator removes and destroys conservation.
Effective counts, cumulative-contribution curves and fractions are legal
only on a declared nonnegative projection of the estimates, and
\code{latent_geometry()} is the named operation that builds one.
}
\details{
The projection is biased and does not conserve. That is not a defect to be
worked around; it is what makes the layer's arithmetic well defined, and it
is why the moved mass is reported per measurement rather than absorbed.
Read the layer descriptively -- which modes carry a form's nonnegative mass
and how concentrated they are -- and read the signed geometry for anything
that has to be unbiased or conserved.
}
\section{Structure}{

One nonnegative spectrum per spatial measurement, with the functionals that
only a nonnegative partition admits and the record of what the projection
cost.
\itemize{
\item \verb{$spectrum}: the projected spectrum, one row per measurement, one column
per root, ordered \code{root1} (largest) through \code{rootq}. Every entry is
nonnegative.
\item \verb{$cumulative}: the cumulative contribution curve
\eqn{C(k) = \sum_{i \le k} \lambda_i / \sum_i \lambda_i}{C(k) = sum_(i <= k) lambda_i / sum_i lambda_i}, columns \code{C1}
through \code{Cq}, so \code{Cq} is \code{1} wherever the curve is defined.
\item \verb{$n_eff}: the participation ratio
\eqn{(\sum_i \lambda_i)^2 / \sum_i \lambda_i^2}{(sum_i lambda_i)^2 / sum_i lambda_i^2} of the projected
spectrum, one per measurement.
\item \verb{$moved_mass}: the absolute mass the projection removed from each
measurement -- the sum of the magnitudes of its negative roots. Zero for
a measurement whose source spectrum was already nonnegative.
\item \verb{$moved_share}: that mass as a fraction of the source spectrum's total
absolute mass, so a measurement that moved a lot in a large form is not
confused with one that moved a little in a small one.
\item \verb{$component}, \verb{$method}: what was projected, and by which named operator.
\item \verb{$index}: the measurement identifiers, carried from the source.
\item \verb{$projection}: the projection receipt -- the operator, the per-measurement
moved mass and share, the counts of clipped and masked measurements, and
the source identity it was derived from.
\item \verb{$receipt}: the execution receipt. Its \verb{$scientific_plan_id} is derived
from the source's and the projection's name, so a latent layer never
shares an identity with the signed estimates behind it, and its
\verb{$task_partition_id} ends in \code{+psd_projection}.
}

Any element not listed here is internal and may change.
}

\section{Masking}{

A measurement whose projected spectrum sums to zero -- every root at or
below zero -- has no nonnegative partition, so its \verb{$n_eff} and its whole
\verb{$cumulative} row are \code{NA} rather than \code{0/0}. \verb{$projection$masked} counts
them. This is the discipline the coherence fraction already uses: a number
that was not earned is withheld, not clamped.

The guard is exact (\verb{> 0}), not a tolerance, and it is the same guard every
per-node fraction in the package uses. A form that is zero only to within
rounding therefore returns \verb{$n_eff} and \verb{$cumulative} computed from that
rounding: a row of roots at \code{1e-17} gives the same \verb{$n_eff} as a row a
hundred million times larger, because the ratio is scale free.
\verb{$moved_share} will read near \code{0.5} for such a row and \verb{$spectrum} will
show the scale, so the object says what happened -- but the effective count
of a form that is numerically zero is noise, and no guard here will tell
you so. Whether these guards should take a relative tolerance is an open
contract decision (\code{design/conservative-geometry-contract.md} §11.4, gap
G3, where a singleton scale's aggregated configuration was measured at
\code{-2.8e-17}); this layer inherits it rather than making a private one.

\verb{$moved_share} is the one quantity that is \emph{not} masked when its
denominator vanishes: a form with no absolute mass at all moved none of it,
so the share is \code{0}. \code{NA} there would read as "we could not tell you how
much was clipped" about a measurement where the answer is known.
}

\section{Refusal}{

A query-first \code{effect_geometry_plan} signals an
\code{effect_capability_refusal} with capability \code{"complete_geometry"}; a
rectangular or non-symmetric form signals \code{"symmetric_self_form"}; a query
readout (\code{contrast_energy()}, \code{rdm()}, \code{rsa()}, \code{crossnobis()}, or a
\code{query_geometry()} view) signals \code{"latent_projection_source"}, because
clamping a contracted value is a per-node total clamp and a different
projection. \code{method = "nearest_psd"} signals
\code{"nearest_psd_projection"}. See \code{\link[=catch_refusal]{catch_refusal()}}.
}

\examples{
domain <- abstract_domain(4, id = "latent-example")
relation <- relation(
  list(
    run1 = rbind(face = c(1, 0, 2, 1), house = c(0, 1, 1, 0),
      tool = c(0.5, -1, 0, 1)),
    run2 = rbind(face = c(0.9, 0.2, 1.7, 0.6), house = c(0.2, 0.8, 1.3, 0.1),
      tool = c(-0.4, 1, 0.2, -0.8))
  ),
  domain = domain
)
geometry <- materialize_geometry(plan_geometry(
  relation, compile_frame(regions(c("v1", "v1", "it", "it")), domain),
  cross_partitions(relation, independence = "independent")
))

# The signed spectrum keeps its negative root; both regions have one.
geometry_spectrum(geometry)$values

# The latent layer truncates it and says what that cost, per region.
latent <- latent_geometry(geometry)
latent
latent$cumulative
latent$projection

# A contracted readout has no spectrum to project, and clamping it would be
# a per-node total clamp: a different operator moving different mass.
effect <- contrast_energy(geometry, c(face = 1, house = -1, tool = 0))
catch_refusal(latent_geometry(effect))$capability
}
\seealso{
\code{\link[=geometry_spectrum]{geometry_spectrum()}} for the signed spectrum this layer projects,
which never truncates a negative root, and \code{\link[=contribution]{contribution()}} for the
additive ledger reading that stays on the signed layer.

Other geometry plans and views: 
\code{\link{aggregate_first}()},
\code{\link{bilinear_query}()},
\code{\link{coherence_spectrum}()},
\code{\link{compute_policy}()},
\code{\link{contrast_energy}()},
\code{\link{contribution}()},
\code{\link{crossnobis}()},
\code{\link{evaluate_geometry}()},
\code{\link{example_fmri_effects}()},
\code{\link{geometry_component}()},
\code{\link{geometry_spectrum}()},
\code{\link{materialize_geometry}()},
\code{\link{plan_crossnobis}()},
\code{\link{plan_geometry}()},
\code{\link{plot_views}},
\code{\link{query_geometry}()},
\code{\link{rdm}()},
\code{\link{rsa}()},
\code{\link{variation_query}()}
}
\concept{geometry plans and views}
