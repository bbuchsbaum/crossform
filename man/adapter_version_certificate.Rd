% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compiler-conformance.R
\name{adapter_version_certificate}
\alias{adapter_version_certificate}
\title{Certify the installed version of an adapter's upstream package}
\usage{
adapter_version_certificate(package, supported)
}
\arguments{
\item{package}{Name of the upstream package the adapter is certified
against.}

\item{supported}{The one version string the adapter has been tested with.}
}
\value{
The installed version of \code{package}, as a character string equal to
\code{supported}. Anything else refuses:
\code{installed_compiler_adapter} when \code{package} is not installed, and
\code{supported_compiler_version} when the installed version is not the
certified one. Both refusals carry the \code{relation_compiler} namespace and
name the certified version in their remedies.
}
\description{
\code{vignette("crossform-extending")} places two obligations on an adapter
author, and this is the first of them: certify against the \emph{installed}
version of the package you adapt, and record what you certified against.
The obligation is part of the protocol, so the refusal that discharges it
is too --- an adapter outside this package raises the same two refusals, in
the same namespace, as the two adapters shipped here.
}
\details{
Call it first, before touching the upstream package, and record the
returned string in the provenance of whatever you build. A version other
than the certified one is refused rather than attempted: an untested
upstream release that still runs is the failure mode this exists to
prevent.
}
\examples{
# crossform certifies its own fmridesign adapter against exactly one
# version; this is the call that enforces it.
if (requireNamespace("fmridesign", quietly = TRUE)) {
  installed <- as.character(utils::packageVersion("fmridesign"))
  print(identical(
    adapter_version_certificate("fmridesign", installed), installed
  ))
}

# An uncertified version is refused, not attempted. The refusal names the
# missing capability, so a caller can branch on the cause.
refusal <- catch_refusal(
  adapter_version_certificate("stats", "0.0.0-never-released")
)
refusal$capability
refusal$remedies

# A package that is not installed at all refuses differently.
catch_refusal(
  adapter_version_certificate("crossformNotAPackage", "1.0.0")
)$capability
}
\seealso{
\code{\link[=compiler_conformance]{compiler_conformance()}}, which checks the receipt an adapter
produced rather than the version that produced it, and
\code{\link[=catch_refusal]{catch_refusal()}} to inspect either refusal.

Other relation planning and fitting: 
\code{\link{compiler_conformance}()},
\code{\link{effect_extractor}()},
\code{\link{estimate_relation}()},
\code{\link{file_matrix_source}()},
\code{\link{fmridesign_design_model}()},
\code{\link{fmrireg_relation}()},
\code{\link{lm_extractor}()},
\code{\link{lm_relation_fit}()},
\code{\link{plan_relation}()},
\code{\link{relation}()},
\code{\link{relation_block}()},
\code{\link{relation_fit}()},
\code{\link{relation_fit_capabilities}()},
\code{\link{relation_plan_receipts}()},
\code{\link{residual_block}()},
\code{\link{residual_df}()},
\code{\link{source_capabilities}()}
}
\concept{relation planning and fitting}
