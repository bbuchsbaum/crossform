% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric-learning.R
\name{compile_metric_schedule}
\alias{compile_metric_schedule}
\title{Compile a frozen-provenance, on-demand metric schedule}
\usage{
compile_metric_schedule(
  recipe,
  statistics,
  at,
  over,
  training = metric_training_policy("exclude_evaluation")
)
}
\arguments{
\item{recipe}{A recipe from `identity_metric()`, `diagonal_precision()`, or
`shrinkage_precision()`.}

\item{statistics}{Canonical `residual_pair_statistics()`.}

\item{at}{The same support-index-backed frame used to accumulate
`statistics`.}

\item{over}{An evaluation `pairing()`.}

\item{training}{A `metric_training_policy()`.}
}
\value{
An `effect_frozen_metric_schedule`.
}
\description{
Compilation binds a metric recipe to canonical residual sufficient
statistics, spatial supports, and evaluation edges. What is frozen is the
estimator identity, training assignment, source revisions, and
regularization policy. Local covariance and precision matrices are derived
when requested and are never retained as a node-by-fold table.
}
\keyword{internal}
