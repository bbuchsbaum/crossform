% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-views.R
\name{population_views}
\alias{population_views}
\alias{contrast_energy.effect_population_result}
\alias{rdm.effect_population_result}
\alias{rsa.effect_population_result}
\alias{contribution.effect_population_result}
\alias{contribution.effect_population_view}
\title{Reader verbs on an estimated population form}
\usage{
\method{contrast_energy}{effect_population_result}(x, weights, remove_univariate = FALSE, term = NULL, ...)

\method{rdm}{effect_population_result}(x, component = NULL, pairs = NULL, normalize = NULL, term = NULL, ...)

\method{rsa}{effect_population_result}(
  x,
  models,
  nuisance = NULL,
  intercept = TRUE,
  component = NULL,
  term = NULL,
  ...
)

\method{contribution}{effect_population_result}(x, by, using = NULL, term = NULL, ...)

\method{contribution}{effect_population_view}(x, by, using = NULL, ...)
}
\arguments{
\item{x}{An \code{effect_population_result} from \code{\link[=estimate_population]{estimate_population()}} or
\code{\link[=materialize_population]{materialize_population()}}, or -- for \code{\link[=contribution]{contribution()}} -- an
\code{effect_population_view} from one of the other three verbs.}

\item{weights}{One finite contrast weight per experimental effect, named or
in the effect order of \verb{$queries}.}

\item{remove_univariate}{Must be omitted or \code{FALSE}; refused exactly as
\code{\link[=contrast_energy]{contrast_energy()}} refuses it.}

\item{term}{Which column of the group model to read, by name or position.
Required when the group model fits more than one; the default \code{~ 1} fits
only \code{(Intercept)}, the group mean, and needs no argument.}

\item{...}{Unused; present so the methods match their generics.}

\item{component}{Must be omitted. The geometry component is fixed when the
population is estimated -- it is \verb{component =} on
\code{\link[=estimate_population]{estimate_population()}} -- because a transported ledger of one component
holds no information about another.}

\item{pairs}{Optional two-column matrix of effect pairs, by name or index.
The default reports every unordered pair.}

\item{normalize}{Must be omitted; refused exactly as \code{\link[=rdm]{rdm()}} refuses it.}

\item{models, nuisance}{Named model and nuisance dissimilarity matrices over
the experimental effects, as \code{\link[=rsa]{rsa()}} takes them.}

\item{intercept}{Whether the RSA design carries an intercept column.}

\item{by}{The grouping of group nodes, as one label per group node (the
sink excluded, since it is always its own group) or one column name of
\code{using} or of the view's \verb{$index}.}

\item{using}{Optional per-group-node metadata table naming the territories
a view cannot carry itself. It is \strong{joined on its \code{node} column}, which
must hold the group node identifiers of \verb{$index}, so row order does not
matter and a table without that column is refused rather than bound by
position. Meaningful only when \code{by} names one of its columns.}
}
\value{
An \code{effect_population_view}: \verb{$values}, one row per group node plus
the sink and one column per view column; \verb{$columns} naming those columns;
\verb{$index}, the group node table with the sink marked and its units; the
\verb{$ledger} name, \verb{$term}, \verb{$semantics}, \verb{$normalization}; a \verb{$receipt}
carrying the transport, the native frame family, the budget certificate,
the normalization and the basis coefficients that reached this view; and
a \verb{$scientific_plan_id} derived from the population plan and the view's
own parameters. \code{as.data.frame()} returns the long table.
}
\description{
\code{contrast_energy()}, \code{rdm()}, \code{rsa()} and \code{contribution()} are S3 generics.
Applied to an \code{\link[=estimate_population]{estimate_population()}} result they read the \emph{group}
coefficients that run already produced: a population view is a fixed linear
combination of estimated query columns, not a second execution, and no
participant's geometry is opened again.
}
\section{Why a combination is exact}{

The query, the transport and the group fit act on the experimental,
spatial and participant axes respectively, so under the OLS default they
commute (\code{population-form-v1} section 3). The fitted coefficient of a
combined query is therefore the same combination of the fitted
coefficients of the bank's queries, exactly rather than approximately.
}

\section{What the estimated basis reaches}{

A result from \code{\link[=estimate_population]{estimate_population()}} (\verb{$basis} \code{"query_bank"}) holds the
group coefficient of \code{K} packed operators
\eqn{\mathrm{svec}(c_k c_k^\top)}{svec(c_k c_k^T)}. A view is derivable
exactly when its own packed operator lies in their span, which is a wider
set than the bank's contrast vectors: \verb{2c} is derivable when \code{c} is in the
bank, and the bank of all \eqn{q(q-1)/2}{q(q-1)/2} pairwise difference
contrasts spans \strong{exactly} the quadratic forms of zero-sum contrasts. So a
full pairwise bank reaches every \code{\link[=rdm]{rdm()}} and every \code{\link[=rsa]{rsa()}} regression over
those effects, and reaches no uncentred contrast at all. (Other banks reach
other things: three uncentred contrasts can reach a fourth. The span is the
criterion; the pairwise case is the one worth stating because it is the one
\code{rdm()} and \code{rsa()} rest on.) Outside the span the verb refuses and names
the re-estimation remedy rather than reporting the nearest thing it can
reach under the query's name.

A result from \code{\link[=materialize_population]{materialize_population()}} (\verb{$basis} \code{"complete_form"}) holds
the assembled packed coefficient form, whose basis is the whole coordinate
system. Every symmetric query is in its span by construction, so these
verbs query the form directly and no span refusal can fire.
}

\section{\code{unit_budget} admits one estimated query at a time}{

Under \code{normalization = "unit_budget"} each query column is divided by \emph{that
query's} own native total (\code{population-form-v1} section 4.1), so the
divisor varies along the query axis and the response is not linear in the
query operator. A view that reads \strong{one} estimated query is exact and is
admitted, including a multiple of one: \verb{s H_k} has ledger \verb{s c_k} over
total \verb{s T_\{ik\}}, so its share is column \code{k} for every nonzero \code{s}, and the
weight is set to one rather than to the \eqn{s}{s} a span solve returns. A
view that \strong{mixes} estimated queries is refused: the mixture carries no
denominator, and a share of nothing is not a share.
}

\section{The sink}{

Every view carries the sink as a row of \verb{$index}, labelled \verb{<sink>}, marked
by \verb{$index$sink} and reported in budget units. It is never a value at a
location; it is there because \code{\link[=contribution]{contribution()}}'s ledger adds up only when
unmapped territory is a number in the table.
}

\section{Labelling}{

A transported component is a \code{native_coherent_ledger} or a
\code{native_configuration_ledger}, never a bare \code{coherent} or \code{configuration}
(\code{population-form-v1} section 8.1). \verb{$ledger} carries the name, \verb{$receipt}
carries it beside the native frame family the ledger belongs to and the
transport that carried it, and no output column or printed label of a
population view uses the bare name.
\code{native_coherent_ledger + native_configuration_ledger = transported_total}
holds exactly; what fails is reading the coherent ledger as a group-node
common mode.
}

\section{Refusals}{

Each is an \code{effect_capability_refusal} in namespace \code{"population_views"}
(see \code{\link[=catch_refusal]{catch_refusal()}}).
\itemize{
\item \code{population_query_in_bank} --- the view's packed operator is outside the
span of the estimated bank. Remedy: re-estimate with the contrast in the
bank.
\item \code{population_view_query_linearity} --- a view mixing estimated queries on
a \code{unit_budget} result, or any normalization no view knows how to combine
under.
\item \code{population_component_fixed_at_estimation} --- \code{component}, which is an
argument of \code{\link[=estimate_population]{estimate_population()}} and not of a view.
\item \code{budget_semantics} --- \code{\link[=contribution]{contribution()}} on a \code{"density"} population.
Density satisfies no conservation law, so a territory sum of densities is
a share of nothing.
\item \code{conservative_frame} --- \code{\link[=contribution]{contribution()}} on a population planned with
\code{allow_nonconservative = TRUE} over a frame that is not column
normalized.
\item \code{sink_is_not_a_territory} --- a \code{by} that labels a group node \verb{<sink>}.
The sink is appended as its own row automatically.
\item \code{nondestructive_decomposition} and \code{guaranteed_psd} --- \code{remove_univariate}
and \code{normalize}, refused for the same reasons the per-participant views
refuse them.
}
}

\examples{
# Three participants on different native frames, two group nodes, and a
# bank spanning all three pairwise contrasts.
effects <- effect_space(c("face", "house", "tool"), basis_id = "popview:v1")
subject <- function(id, n) {
  domain <- abstract_domain(n, coordinates = cbind(x = seq_len(n) - 1),
    feature_ids = paste0("f", seq_len(n)), id = id)
  values <- function(divisor) matrix(
    seq_len(3 * n) / (n * divisor), 3, n,
    dimnames = list(c("face", "house", "tool"), NULL)
  )
  rel <- relation(list(run1 = values(1), run2 = values(1.4)),
    effects = effects, domain = domain)
  plan_geometry(rel, compile_frame(voxelwise(), domain),
    cross_partitions(rel))
}
carrier <- function(n) anatomical_transport(
  native_coords = cbind(seq_len(n) - 1), group_coords = cbind(c(0, 4)),
  semantics = "budget"
)
sizes <- c(s01 = 5L, s02 = 6L, s03 = 7L)
subjects <- stats::setNames(
  lapply(names(sizes), function(id) subject(id, sizes[[id]])), names(sizes)
)
fit <- estimate_population(
  plan_population(subjects, lapply(sizes, carrier)),
  rbind(`face-house` = c(1, -1, 0), `face-tool` = c(1, 0, -1),
    `house-tool` = c(0, 1, -1))
)

# One contrast, selected out of the estimated bank.
contrast_energy(fit, c(face = 1, house = -1, tool = 0))

# The three distances, and an RSA regression on them: both are inside the
# span of a full pairwise bank, so neither needs a second run.
rdm(fit)$values
animacy <- matrix(c(0, 0, 1, 0, 0, 1, 1, 1, 0), 3, 3,
  dimnames = list(c("face", "house", "tool"), c("face", "house", "tool")))
as.data.frame(rsa(fit, models = list(animacy = animacy)))

# The ledger adds up over the group nodes and the sink.
ledger <- contribution(fit, by = c("anterior", "posterior"))
ledger

# An uncentred contrast is outside the span of a zero-sum bank, and is
# refused with the re-estimation remedy rather than projected onto it.
refusal <- catch_refusal(contrast_energy(fit, c(1, 1, 0)))
refusal$capability
}
\references{
\code{design/population-form-contract.md} (\code{population-form-v1}),
sections 2, 3, 4 and 8.
}
\seealso{
\code{\link[=estimate_population]{estimate_population()}} and \code{\link[=materialize_population]{materialize_population()}} for the
runs these read, and \code{\link[=contrast_energy]{contrast_energy()}}, \code{\link[=rdm]{rdm()}}, \code{\link[=rsa]{rsa()}} and
\code{\link[=contribution]{contribution()}} for the per-participant verbs of the same names.

Other population transports: 
\code{\link{anatomical_transport}()},
\code{\link{estimate_population}()},
\code{\link{external_transport}()},
\code{\link{heterogeneity}()},
\code{\link{location_transport}()},
\code{\link{materialize_population}()},
\code{\link{plan_population}()},
\code{\link{population_uncertainty}()},
\code{\link{transport_values}()}
}
\concept{population transports}
