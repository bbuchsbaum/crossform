% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-bootstrap.R
\name{population_wild_bootstrap}
\alias{population_wild_bootstrap}
\title{Null-imposed subject-level wild bootstrap for population coefficients}
\usage{
population_wild_bootstrap(
  x,
  contrast,
  null = 0,
  replicates = 999L,
  seed = NULL,
  weights = c("rademacher", "mammen"),
  level = 0.95,
  leverage_tolerance = 1e-08
)
}
\arguments{
\item{x}{An \code{effect_population_result} from \code{\link[=estimate_population]{estimate_population()}}.}

\item{contrast}{One population coefficient name, or a numeric vector with
one weight per model term. Named weights are aligned by term name.}

\item{null}{Finite scalar value of the tested coefficient contrast.}

\item{replicates}{Number of bootstrap replicates, at least 99.}

\item{seed}{Required nonnegative integer random seed.}

\item{weights}{Wild-weight distribution: \code{"rademacher"} or \code{"mammen"}.}

\item{level}{Nominal two-sided test level used for the absolute bootstrap-t
critical value and \verb{$reject} diagnostic.}

\item{leverage_tolerance}{Positive tolerance below which \eqn{1-h_i}{1-h_i}
is treated as numerically zero.}
}
\value{
An \code{effect_population_wild_bootstrap} carrying the explicit null,
observed and replicate HC3-t statistics, shared subject weights,
replicate-level failures, plus-one p-values, critical values, rejection
flags, Monte Carlo SEs, exact coverage, conditioning and provenance.
}
\description{
\code{population_wild_bootstrap()} tests one explicit linear contrast of the
population-model coefficients against one explicit null at every admitted
node and query. It uses a null-imposed, HC3-studentized wild bootstrap. One
random weight is drawn per planned subject and replicate and reused across
\strong{all} nodes and queries, preserving the within-subject dependence of the
jointly analysed result.
}
\section{Algorithm and null}{

For coefficient contrast \eqn{c}{c} and null \eqn{c'\beta = b_0}{c' beta =
b0}, each cell is first fit under the linear restriction. Bootstrap response
\eqn{y^* = X\hat\beta_R + w_i e_{Ri}/(1-h_i)}{y* = X beta_R + w_i e_Ri /
(1-h_i)} uses the restricted residual and its HC3 leverage adjustment. Each
replicate is refit without the restriction and studentized by its own HC3
covariance. The two-sided p-value uses the plus-one rule, so it is never
zero; \verb{$monte_carlo_se} reports its finite-replication uncertainty.

\code{"rademacher"} weights are symmetric \eqn{-1,+1}{-1,+1} draws with equal
probability. \code{"mammen"} uses the two-point, mean-zero, variance-one Mammen
distribution, whose third moment also equals one; it is provided for
asymmetric-error sensitivity analyses, not as an automatic improvement.
}

\section{Coverage, failures, and conditioning}{

The exact subject set from \verb{$coverage} is used separately at every cell,
while the weights come from one shared planned-subject matrix. Cells with
fewer than six available subjects, rank deficiency, no residual df,
leverage within \code{leverage_tolerance} of one, or a nonpositive observed HC3
standard error are refused with a status and reason. Replicate failures are
retained in \verb{$replicate_failure_reason}; a cell is reported only when at
least 90 percent and at least 20 replicates succeed. All inference is
conditional on the realized transport and coverage. It does not propagate
uncertainty from estimating either. Cross-fitting can limit circular reuse
of responses, but does not make this bootstrap marginal over transport
estimation or fold assignment.

The returned \verb{$weights}, \verb{$seed}, \verb{$rng}, \verb{$weight_signature}, replicate
statistics, success flags, failure reasons, contrast, null and parent
receipt provide full resampling provenance. The caller's random-number state
is restored on exit.
}

\seealso{
\code{\link[=population_uncertainty]{population_uncertainty()}} for analytic classical and HC3
covariance without resampling.

Other population transports:
\code{\link{anatomical_transport}()},
\code{\link{estimate_population}()},
\code{\link{external_transport}()},
\code{\link{heterogeneity}()},
\code{\link{location_transport}()},
\code{\link{materialize_population}()},
\code{\link{plan_population}()},
\code{\link{population_prevalence}()},
\code{\link{population_uncertainty}()},
\code{\link{population_views}},
\code{\link{transport_values}()}
}
\concept{population transports}
