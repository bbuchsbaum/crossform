% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transport.R
\name{external_transport}
\alias{external_transport}
\title{Admit a transport built outside crossform}
\usage{
external_transport(
  P,
  semantics,
  provenance,
  native_index = NULL,
  group_index = NULL,
  row_mass = NULL,
  tolerance = 1e-09
)
}
\arguments{
\item{P}{The \code{n_native} by \code{m} nonnegative group block, as a base matrix or
a \code{Matrix}. Dimension names, when present, become the default node
identifiers.}

\item{semantics}{\code{"budget"} or \code{"density"}. Required; see
\code{\link[=location_transport]{location_transport()}}.}

\item{provenance}{A list carrying at least \code{details}; \code{method} defaults to
\code{"external"}.}

\item{native_index, group_index}{Optional index tables or node-identifier
vectors overriding the dimension names.}

\item{row_mass}{Optional declared positive territory measure, one entry per
native node.}

\item{tolerance}{Positive row-sum tolerance, as in \code{\link[=location_transport]{location_transport()}}.}
}
\value{
An \code{effect_location_transport}.
}
\description{
\code{crossform} does not learn transports. \code{external_transport()} is the door a
transport built elsewhere --- by a registration pipeline, a parcellation
tool, or a functional alignment method --- comes through: a thin wrapper
over \code{\link[=location_transport]{location_transport()}} that takes the group block as it stands, reads
node identifiers from its dimension names when they are there, and requires
the provenance that makes the operator auditable.
}
\details{
Declaring \code{method = "external"} says only that the operator was built
outside this package. A transport that was fitted to response data is
\code{"functional"} whichever program fitted it, and must name its \code{cross_fit}
partitions; \code{"external"} is not a way around that.
}
\examples{
# A partial-volume operator produced by some other program, with its node
# names already on the dimensions.
P <- matrix(c(0.7, 0.2, 0, 0.3, 0.8, 0.5), nrow = 3,
  dimnames = list(c("n1", "n2", "n3"), c("A", "B")))
transport <- external_transport(
  P, semantics = "density",
  provenance = list(details = "partial-volume warp from atlas-tool 2.1")
)
transport

# Density divides the transported budget by the transported row mass; with
# the default unit row mass that is the transported node count.
transport_values(transport, c(1, 1, 1))
}
\seealso{
\code{\link[=location_transport]{location_transport()}}, \code{\link[=anatomical_transport]{anatomical_transport()}}.

Other population transports: 
\code{\link{anatomical_transport}()},
\code{\link{location_transport}()},
\code{\link{plan_population}()},
\code{\link{transport_values}()}
}
\concept{population transports}
