% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compiler.R
\name{materialize_geometry}
\alias{materialize_geometry}
\title{Materialize a complete cross-generalized geometry}
\usage{
materialize_geometry(
  x,
  at = NULL,
  over = NULL,
  storage = c("memory", "block"),
  storage_path = NULL,
  compute = NULL,
  reporter = NULL
)
}
\arguments{
\item{x}{An \code{effect_geometry_plan} or, for compatibility, an
\code{effect_relation}.}

\item{at}{A compiled additive \code{effect_frame}, required only with a relation.}

\item{over}{An \code{effect_pairing}, required only with a relation.}

\item{storage}{Either \code{"memory"} or \code{"block"}.}

\item{storage_path}{Durable directory for block-backed geometry.}

\item{compute}{A sequential \code{compute_policy()} when \code{x} is a relation. A
compiled plan already owns this choice.}

\item{reporter}{Optional nonsemantic coordinator-side event reporter.}
}
\value{
A complete \code{effect_geometry}.
}
\description{
\code{materialize_geometry()} explicitly materializes complete total and
coherent packed geometry. Prefer \code{\link[=plan_geometry]{plan_geometry()}} followed by
\code{\link[=evaluate_geometry]{evaluate_geometry()}} when a fixed query is sufficient. The relation
compatibility form compiles that same plan first; it does not use a second
execution path.
}
