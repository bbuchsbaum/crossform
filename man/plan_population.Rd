% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-plan.R
\name{plan_population}
\alias{plan_population}
\title{Plan a population form over transported conservative geometry}
\usage{
plan_population(
  subjects,
  transport,
  model = ~1,
  data = NULL,
  normalization = c("none", "unit_budget", "precision_weighted"),
  coverage_policy = c("all_planned", "available_at_node"),
  coverage_tolerance = 0,
  compute = compute_policy(),
  allow_nonconservative = FALSE
)
}
\arguments{
\item{subjects}{A named list of compiled \code{effect_geometry_plan} values from
\code{\link[=plan_geometry]{plan_geometry()}}, one per participant, over conservative frames on a
common experimental space. Names are participant identifiers and bind
\code{transport} and \code{data}. The list is stored in a canonical name order, so
the plan's identity does not depend on the order it was given in.}

\item{transport}{A named list of \code{\link[=location_transport]{location_transport()}} values, one per
participant with matching names, all landing on one shared group node set
under one \code{semantics}.}

\item{model}{A one-sided group-level formula evaluated against one row per
participant. The default \code{~ 1} is the group mean.}

\item{data}{A data frame with one row per participant, identified by a
\code{subject} column or by row names holding the participant identifiers.
\code{NULL} (the default) supplies the empty table an intercept-only model
needs.}

\item{normalization}{How incommensurable per-participant budgets are made
commensurable: \code{"none"} (the default), \code{"unit_budget"}, or
\code{"precision_weighted"} (gated, see above).}

\item{coverage_policy}{Population target under incomplete ordinary-node
coverage. "all_planned" (the default) returns a coefficient only when
every planned participant is available at that node and query.
"available_at_node" fits the explicitly selected participant set and
records that different target at every cell.}

\item{coverage_tolerance}{Nonnegative relative transported-mass threshold.
Subject i covers ordinary node j when its transported row mass is greater
than coverage_tolerance times its total declared row mass. The sink is
observed for every admitted subject because zero sink budget is meaningful.}

\item{compute}{A \code{\link[=compute_policy]{compute_policy()}}. It enters the plan's physical
signature, never its scientific identity.}

\item{allow_nonconservative}{Plan on non-conservative subject frames
anyway. Recorded on the plan and part of its scientific identity, because
the result is a different estimand rather than a relaxed setting on the
same one.}
}
\value{
An \code{effect_population_plan}: a sealed list carrying \verb{$subjects},
\verb{$transport}, the shared \verb{$group_index}, the transport \verb{$semantics}, the
\verb{$model} record (formula, canonical text, term labels, model matrix, its
pivoted \verb{$qr}, \verb{$rank} and \verb{$pivot}, and per-covariate content digests),
\verb{$data}, \verb{$normalization}, \verb{$coverage_policy}, \verb{$coverage_tolerance},
the subject-by-node \verb{$operator_mass} and \verb{$operator_coverage}, the \verb{$fit}
marker, \verb{$allow_nonconservative}, a per-participant \verb{$subject_index} audit
table (including transport source, fixed-versus-estimated status, fitting
sample, cross-fitting folds, conditional inference scope, and whether
transport uncertainty was propagated), \verb{$compute}, the
\verb{$scientific_plan_id} naming the estimand, and the \verb{$signature} covering
how it will be executed.
}
\description{
\code{plan_population()} names a group-level estimand: each participant's
compiled conservative geometry, one declared \code{\link[=location_transport]{location_transport()}} each
onto one shared set of group nodes, and one group-level model over the
subject axis. It reads no data and fits nothing --- it is a sealed
declaration of what a group number will mean, and a set of refusals for
the combinations that would make that meaning false.
}
\section{What the plan fixes}{

Three choices decide what the group number \emph{is}, and all three enter the
plan's scientific identity rather than being applied silently.
\itemize{
\item \strong{The transport.} The group form is not "the population geometry"; it
is the population geometry \emph{as resolved by} \code{P}. Two transports give two
different, equally valid estimands.
\item \strong{The normalization.} Participants have incommensurable native budgets,
so combining them requires a declared choice: \code{"none"} is the mean
subject ledger in native evidence units (participants with more evidence
weigh more), and \code{"unit_budget"} is the mean attribution \emph{share}, in
which every participant contributes budget one regardless of how much
evidence they have. On the contract's own fixture the two disagree by up
to 69 \% and pick different group nodes as the maximum.
\item \strong{The fit and its order.} The group fit is OLS. Subject-constant
weights are the commuting class: query, transport and fit are then linear
maps on distinct axes and every evaluation order gives the same answer.
With heterogeneous native frames there is no common node axis at all, so
transport must precede the fit; the order is recorded rather than chosen.
}
}

\section{Refusals}{

Each is an \code{effect_capability_refusal} carrying the missing capability, all
unmet reasons, and a remedy (see \code{\link[=catch_refusal]{catch_refusal()}}).
\itemize{
\item \code{common_experimental_space} --- participants' geometry indexed by
different effects, orders, bases, units or scales.
\item \code{symmetric_packed_population} --- a rectangular cross-axis subject plan.
\item \code{fixed_metric_population} --- a subject plan on a learned neural metric.
\item \code{conservative_subject_geometry} --- a subject frame that is not
conservative, either by declaration or after a diagonal metric fold,
unless \code{allow_nonconservative = TRUE}.
\item \code{aligned_subject_transports} --- transport and subject names that do not
match, or a transport whose native rows do not count the participant's
frame measurements.
\item \code{shared_group_nodes} --- transports landing on different group node sets,
or declaring different \code{semantics}.
\item \code{aligned_subject_rows} --- a \code{data} table whose rows do not name their
participants.
\item \code{identified_group_model} --- a two-sided formula, an absent or incomplete
covariate, a non-finite design, or a rank-deficient design (the refusal
names the aliased columns).
\item \code{precision_weighted_normalization} --- see below.
}
}

\section{The \code{precision_weighted} gate}{

\code{"precision_weighted"} weighs each participant by
\eqn{\pi_i = 1/\mathrm{Var}(\hat T_i)}, the precision of a \emph{conserved
budget}. That variance needs the full cross-node sampling covariance rather
than a sum of per-node margins, and crossform has no route that produces
one: \code{\link[=sampling_covariance]{sampling_covariance()}} reads a query bank at one measurement and
refuses \code{scope = "cross_measurement"} for want of a spatial model. The mode
is therefore refused rather than approximated --- overlapping conservative
rows are strongly correlated, so a precision assembled from per-node
margins would reweight participants by the wrong quantity, and the contract
measures the three normalizations disagreeing by up to 94 \%. The mode stays
in the closed set, and in this argument's default, because the set \emph{is} the
plan identity; what is gated is admitting it.

This gate does not apply to the default unweighted node-wise OLS. Each
node-query cell is fitted across participants and needs no model for
covariance between nodes. Future covariance-dependent operations must meet
\code{design/cross-node-covariance-contract.md}: dense/sparse representation,
index binding, symmetry and PSD checks, error-model provenance, and
compute-budget guardrails are mandatory.
}

\examples{
# Two participants, different native frames, one shared pair of group nodes.
effects <- effect_space(c("face", "house"), basis_id = "conditions:v1")
subject_plan <- function(id, n) {
  domain <- abstract_domain(n, coordinates = cbind(x = seq_len(n) - 1),
    feature_ids = paste0("f", seq_len(n)), id = id)
  values <- function() matrix(seq_len(2 * n) / n, 2, n,
    dimnames = list(c("face", "house"), NULL))
  rel <- relation(list(run1 = values(), run2 = values() / 2),
    effects = effects, domain = domain)
  plan_geometry(rel, compile_frame(voxelwise(), domain),
    cross_partitions(rel))
}
carrier <- function(n) anatomical_transport(
  native_coords = cbind(seq_len(n) - 1), group_coords = cbind(c(0, 3)),
  semantics = "budget"
)

plan <- plan_population(
  subjects = list(s01 = subject_plan("s01", 5L), s02 = subject_plan("s02", 6L)),
  transport = list(s01 = carrier(5L), s02 = carrier(6L))
)
plan
plan$subject_index[, c("subject", "declared_normalization", "sink_territory")]

# A group covariate: rows name the participant they belong to.
covariates <- data.frame(age = c(24, 31), row.names = c("s01", "s02"))
with_age <- plan_population(
  subjects = list(s01 = subject_plan("s01", 5L), s02 = subject_plan("s02", 6L)),
  transport = list(s01 = carrier(5L), s02 = carrier(6L)),
  model = ~ age, data = covariates
)
with_age$model$columns

# The transport is part of the estimand, so changing it moves the identity.
identical(plan$scientific_plan_id, with_age$scientific_plan_id)
}
\references{
\code{design/population-form-contract.md} (\code{population-form-v1}),
sections 1--4 and 9.
}
\seealso{
\code{\link[=location_transport]{location_transport()}} and \code{\link[=anatomical_transport]{anatomical_transport()}} for the
operator, \code{\link[=transport_values]{transport_values()}} for the contraction it performs, and
\code{\link[=plan_geometry]{plan_geometry()}} for the per-participant input.

Other population transports: 
\code{\link{anatomical_transport}()},
\code{\link{estimate_population}()},
\code{\link{external_transport}()},
\code{\link{heterogeneity}()},
\code{\link{location_transport}()},
\code{\link{materialize_population}()},
\code{\link{population_prevalence}()},
\code{\link{population_uncertainty}()},
\code{\link{population_views}},
\code{\link{population_wild_bootstrap}()},
\code{\link{transport_values}()}
}
\concept{population transports}
