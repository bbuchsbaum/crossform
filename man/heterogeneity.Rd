% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-heterogeneity.R
\name{heterogeneity}
\alias{heterogeneity}
\alias{heterogeneity.effect_population_plan}
\alias{heterogeneity.effect_population_result}
\title{Decompose a population form into consensus and heterogeneity}
\usage{
heterogeneity(x, ...)

\method{heterogeneity}{effect_population_plan}(
  x,
  estimator = c("cross_fit", "plug_in"),
  component = c("total", "coherent", "configuration"),
  nodes = NULL,
  modes = NULL,
  partitions = NULL,
  projection = "psd_projection",
  coordinate_tile = NULL,
  ...
)

\method{heterogeneity}{effect_population_result}(
  x,
  estimator = c("plug_in", "cross_fit"),
  nodes = NULL,
  modes = NULL,
  projection = "psd_projection",
  ...
)
}
\arguments{
\item{x}{An \code{effect_population_plan} from \code{\link[=plan_population]{plan_population()}}, read through
the streamed complete-form route, or an \code{effect_population_result} from
\code{\link[=estimate_population]{estimate_population()}}, read in the space of its own query bank.}

\item{...}{Passed to the method; every method refuses an unmatched argument
rather than absorbing it.}

\item{estimator}{\code{"cross_fit"} (the default on a plan) or \code{"plug_in"}. See
the section above; the choice is part of the record's identity.}

\item{component}{Which component of each participant's conservative geometry
to carry: \code{"total"} (the default), \code{"coherent"} or \code{"configuration"}, as
in \code{\link[=materialize_population]{materialize_population()}}.}

\item{nodes}{Group nodes whose mode geometry is reconstructed, by identifier
or position. \code{NULL}, the default, reconstructs none: the Gram, the
spectrum and the loadings come out of the first pass, and the mode forms
cost a second one.}

\item{modes}{Number of leading modes to reconstruct at those nodes. \code{NULL}
takes \verb{min(3, residual df)}.}

\item{partitions}{Optional list of two character vectors naming the
partition halves the cross-fit reads across. \code{NULL} interleaves each
participant's own partitions, which balances the halves against anything
drifting across a session.}

\item{projection}{The named PSD projection the latent layer is built with,
from \code{\link[=latent_geometry]{latent_geometry()}}'s closed set. It is validated on either method
and enters the record's identity, but it does no work on a result: the
query-space route is plug-in, so that record has no latent layer to
project.}

\item{coordinate_tile}{Optional positive number of packed coordinates held
in flight at once, as in \code{\link[=materialize_population]{materialize_population()}}. The tile bounds
memory and is not part of the estimand.}
}
\value{
An \code{effect_population_heterogeneity}: a sealed record carrying the
\verb{$gram}, its signed \verb{$spectrum} and subject \verb{$loadings}, the \verb{$mode_forms}
reconstructed at the selected \verb{$nodes}, the \verb{$latent} projection, and a
\verb{$receipt} recording the estimator, the partition split, the streaming
bound and every participant's read. \code{as.data.frame()} returns the subject
loadings in long form.

\verb{$mode_forms} is \code{NA} at a \verb{(node, coordinate)} cell the Gram could not
see --- a group node reached by no native mass under density semantics ---
because zero there is a form and would read as agreement rather than as
absence. \verb{$receipt$unresolved_columns} counts them.
}
\description{
\code{heterogeneity()} builds the subject Gram of
\code{design/population-form-contract.md} (\code{population-form-v1}) sections 5 and
6: the \eqn{N \times N}{N x N} matrix of inner products between
participants' deviations from the group fit, its spectrum, the subject
loadings on its modes, and --- at group nodes the caller names --- the
geometry of those modes.
}
\details{
Geometry-space covariance across \code{N} participants has rank at most
\eqn{N-1}{N-1}, so the whole spectrum lives in an \eqn{N \times N}{N x N}
matrix and the \eqn{D \times D}{D x D} covariance never has to be formed.
The Gram is additive over group nodes, so it accumulates while streaming and
the \eqn{N \times m \times p}{N x m x p} stack is never built either.
}
\section{What the deviations are}{

At group node \code{u} the record holds \verb{K_u = D_u D_u^T}, where \code{D_u} is the
\eqn{N \times p}{N x p} matrix of participant deviations \emph{from the fitted
value under the group model}, in the packed \code{svec} coordinates of
\code{effect-form-v1} section 3. Under the default \code{~ 1} design that is each
participant minus the group mean; under any other design it is what is left
after the covariates. The global Gram is \verb{sum_u K_u}, divided by the residual
degrees of freedom.

The \code{sqrt(2)} on the packed off-diagonals is load-bearing: only with it is
the Euclidean inner product of two packed rows the Frobenius inner product
of the two forms. A Gram built on a naive \code{upper.tri} packing is off by an
\eqn{O(1)}{O(1)} amount and is not a Gram of geometries at all.

The sink is excluded. It is unmapped territory reported in budget units
under either semantics, so an inner product including it would add a mass to
a density; \verb{$receipt$gram$sink_excluded} records the exclusion and the sink
budget stays readable on \verb{$receipt$sink_budget}.
}

\section{The two estimators}{

The plug-in Gram books every participant's own within-subject sampling noise
as between-participant heterogeneity. Measured over 2 000 Monte Carlo
replications of the contract's fixture, it inflates the heterogeneity trace
by \strong{+62.7 \%} (section 6.2). The cross-fitted Gram takes every inner
product across two independent halves of each participant's partitions,
\eqn{\widehat\Gamma_{ii'} = \tfrac12(\langle Z_i^{A},Z_{i'}^{B}\rangle +
\langle Z_i^{B},Z_{i'}^{A}\rangle)}{Gamma_ii' = (<Z_i^A, Z_i'^B> + <Z_i^B,
Z_i'^A>)/2}, which is unbiased for the Gram of the noiseless participant
vectors --- on the diagonal too, because the two halves are independent.

The cross-fitted Gram is \strong{indefinite by construction} (measured in 100 \%
of the contract's replications), and a single draw of its trace is not an
estimate of the between-participant trace: measured standard deviation
\code{3.58} against a mean of \code{10.81}. Both facts are reported and never
repaired.

\code{estimator = "cross_fit"} re-plans each participant over two disjoint halves
of their partitions, so it needs at least four partitions per participant and
costs a second streaming read. \code{estimator = "plug_in"} reads one pass and is
admissible for loading \emph{directions}, which section 6.3 measures surviving
the inflation; its \verb{$latent} layer is \code{NULL}, and the refusal that says why
travels on \verb{$receipt$latent_refusal} as a record rather than an error.

\strong{One departure from section 6.4, stated rather than buried.} Its literal
text refuses "any spectrum, variance-explained figure or \code{n_eff}" from the
plug-in Gram; \code{crossform} reports the plug-in \strong{spectrum} and refuses the
other two. The Gram itself is a field of the record and the validator
re-derives its eigenvalues from it, so withholding them would be theatre
rather than discipline --- while a fraction of an inflated whole, or an
effective count over noise modes, is a number that misleads whatever it is
labelled. The spectrum is reported beside the estimator's name, the
measured inflation and the refusal, never alone.

Under \code{"cross_fit"} the mode \emph{geometry} is reconstructed from the average of
the two halves' deviations --- a plug-in reconstruction of a direction,
which section 6.3 admits provided the source is named. It is named on
\verb{$receipt$gram$mode_reconstruction} and on the printed \code{modes} line. The
eigenvalues beside it still come from the cross-fitted Gram.
}

\section{The latent layer}{

The heterogeneity spectrum is signed. Every functional that treats it as a
nonnegative partition --- a fraction, a cumulative curve, an effective mode
count --- is defined only on a named projection of it, exactly as
\code{\link[=latent_geometry]{latent_geometry()}} requires one level down (section 6.5). \verb{$latent} carries
the projected spectrum, its cumulative curve, its \code{n_eff}, and the mass the
projection moved; \verb{$spectrum} stays signed and carries none of them.
}

\section{Refusals}{

Each is an \code{effect_capability_refusal} (see \code{\link[=catch_refusal]{catch_refusal()}}).
\itemize{
\item \code{cross_fitted_subject_gram} --- fewer than four partitions, a declared
split that shares a partition between halves or names one a participant
does not carry, or a metric schedule that cannot be re-planned onto a
half.
\item \code{identified_heterogeneity} --- a saturated group design, where no residual
degree of freedom is left for participants to differ in.
\item \code{query_space_cross_fit} --- \code{estimator = "cross_fit"} on an estimated
result, which carries one partition split and cannot supply a second.
\item \code{complete_form_normalization} --- a plan declaring \code{"unit_budget"}, as in
\code{\link[=materialize_population]{materialize_population()}}.
\item \code{selected_group_nodes} --- a \code{nodes} identifier the population does not
carry.
}

One further refusal is \emph{recorded} rather than raised, because the record it
belongs to is still worth having: \code{plug_in_spectrum_functionals} sits on
\verb{$receipt$latent_refusal} of every plug-in record and carries the reasons
and remedies for the absent \verb{$latent} layer.
}

\examples{
# Four participants over four runs, so each cross-fit half holds two.
effects <- effect_space(c("face", "house"), basis_id = "heterogeneity:v1")
subject <- function(id, n, gain) {
  domain <- abstract_domain(n, coordinates = cbind(x = seq_len(n) - 1),
    feature_ids = paste0("f", seq_len(n)), id = id)
  run <- function(seed) {
    set.seed(seed)
    matrix(gain * stats::rnorm(2 * n), 2, n,
      dimnames = list(c("face", "house"), NULL))
  }
  rel <- relation(
    list(run1 = run(1), run2 = run(2), run3 = run(3), run4 = run(4)),
    effects = effects, domain = domain
  )
  plan_geometry(rel, compile_frame(voxelwise(), domain),
    cross_partitions(rel))
}
carrier <- function(n) anatomical_transport(
  native_coords = cbind(seq_len(n) - 1), group_coords = cbind(c(0, 3)),
  semantics = "budget", radius = 2
)
sizes <- c(s01 = 5L, s02 = 6L, s03 = 7L, s04 = 8L)
gains <- c(s01 = 1, s02 = 1.4, s03 = 0.7, s04 = 1.1)
subjects <- stats::setNames(lapply(names(sizes), function(id)
  subject(id, sizes[[id]], gains[[id]])), names(sizes))
plan <- plan_population(subjects, lapply(sizes, carrier))

# The cross-fitted subject Gram: one N-by-N matrix, streamed. Naming a
# group node reconstructs that node's mode geometry in a second pass.
split <- heterogeneity(plan, nodes = "group1", modes = 1L)
split

# Signed and routinely indefinite; the nonnegative functionals live on the
# named projection beside it.
split$spectrum
split$latent$n_eff
split$latent$moved_mass

# Subject loadings on the leading mode, and that mode's geometry at the
# named group node, unpacked from its `svec` coordinates.
split$loadings[, "mode1"]
crossform:::.unsvec_symmetric(split$mode_forms["group1", "mode1", ], 2L)
split$receipt$gram$mode_reconstruction

# The plug-in Gram costs one pass instead of two and is admissible for
# directions; it carries no `n_eff`, and says why.
plug <- heterogeneity(plan, estimator = "plug_in")
is.null(plug$latent)
plug$receipt$latent_refusal$capability
}
\references{
\code{design/population-form-contract.md} (\code{population-form-v1}),
sections 5 and 6.
}
\seealso{
\code{\link[=estimate_population]{estimate_population()}} and \code{\link[=materialize_population]{materialize_population()}} for the
consensus half of the same decomposition, and \code{\link[=latent_geometry]{latent_geometry()}} for the
single-participant form of the projection discipline.

Other population transports: 
\code{\link{anatomical_transport}()},
\code{\link{estimate_population}()},
\code{\link{external_transport}()},
\code{\link{location_transport}()},
\code{\link{materialize_population}()},
\code{\link{plan_population}()},
\code{\link{population_prevalence}()},
\code{\link{population_uncertainty}()},
\code{\link{population_views}},
\code{\link{transport_values}()}
}
\concept{population transports}
