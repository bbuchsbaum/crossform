% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relation-plan.R
\name{estimate_relation}
\alias{estimate_relation}
\title{Estimate a planned relation family}
\usage{
estimate_relation(x, ...)
}
\arguments{
\item{x}{An \code{\link[=plan_relation]{plan_relation()}} result.}

\item{...}{Reserved for future execution policies.}
}
\value{
An \code{effect_relation_fit} whose \verb{$signature} binds the relation plan,
source revisions, realized row lineage, and design receipts, and whose
\verb{$provenance} records \code{relation_plan_id}, \code{design_receipt_ids},
\code{study_id}, and \code{observation_model_id}.
}
\description{
This is the authorized first-moment execution verb. It emits the existing
\code{effect_relation_fit} contract, so every second-moment plan and view remains
unchanged.
}
\examples{
set.seed(1)
domain <- abstract_domain(3L, id = "estimate-relation-example")
index <- observation_index(paste0("scan-", 1:4), "run-1")
facts <- study(observations(
  list(`run-1` = matrix(rnorm(12), 4L, 3L)), list(`run-1` = index), domain
))
design <- cbind(face = c(1, 0, 1, 0), body = c(0, 1, 0, 1))
rownames(design) <- paste0("scan-", 1:4)
target <- rbind(`face-body` = c(1, -1))
colnames(target) <- colnames(design)
plan <- plan_relation(
  facts, raw_design_model(list(`run-1` = design)), raw_effect_map(target),
  observation_model("ols", sampling_unit = "scan")
)

# Reading neural values happens here, and only here.
fit <- estimate_relation(plan)
round(relation_block(fit, "run-1", 1:3), 3)

# A fixed observation model earns the residual channel, and the fit records
# which plan produced it.
relation_fit_capabilities(fit)$residual_blocks
identical(fit$provenance$relation_plan_id, plan$relation_plan_id)
}
\seealso{
\code{\link[=plan_relation]{plan_relation()}} for the plan it executes, \code{\link[=fmrireg_relation]{fmrireg_relation()}}
for the external point-parity adapter, and \code{\link[=plan_geometry]{plan_geometry()}} for the
second-moment question that follows.

Other relation planning and fitting: 
\code{\link{adapter_version_certificate}()},
\code{\link{compiler_conformance}()},
\code{\link{effect_extractor}()},
\code{\link{file_matrix_source}()},
\code{\link{fmridesign_design_model}()},
\code{\link{fmrireg_relation}()},
\code{\link{lm_extractor}()},
\code{\link{lm_relation_fit}()},
\code{\link{plan_relation}()},
\code{\link{relation}()},
\code{\link{relation_block}()},
\code{\link{relation_fit}()},
\code{\link{relation_fit_capabilities}()},
\code{\link{relation_plan_receipts}()},
\code{\link{residual_block}()},
\code{\link{residual_df}()},
\code{\link{source_capabilities}()}
}
\concept{relation planning and fitting}
