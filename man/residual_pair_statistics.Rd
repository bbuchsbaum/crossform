% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residual-statistics.R
\name{residual_pair_statistics}
\alias{residual_pair_statistics}
\title{Accumulate canonical residual pair sufficient statistics}
\usage{
residual_pair_statistics(
  x,
  at,
  partitions = NULL,
  workspace_bytes = 512 * 1024^2
)
}
\arguments{
\item{x}{An \code{effect_relation_fit} with residual-block capability.}

\item{at}{A compiled frame carrying an explicit support index, such as a
compiled searchlight frame. An internal \code{effect_support_index} is also
accepted.}

\item{partitions}{Optional relation partitions. They are canonicalized to
relation order.}

\item{workspace_bytes}{Positive crossform-owned workspace budget.}
}
\value{
An \code{effect_residual_pair_statistics} object. \verb{$pair_i}/\verb{$pair_j}
list the coexisting feature pairs, \verb{$partitions} names the partitions,
\verb{$atomic} holds one \verb{$cross_products} vector and \verb{$residual_df} per
partition, and \verb{$numerical_contract} records the fixed tile shape.
\verb{$execution} diagnostics are excluded from the scientific \verb{$signature}.
}
\description{
Computes residual cross-products only for neural feature pairs that coexist
in at least one requested support. The GEMM tile shape is derived from the
support topology and is independent of \code{workspace_bytes}; the budget changes
only how many already-computed residual tiles may be cached. Consequently,
changing the workspace plan cannot change the accumulated floating-point
values.
}
\examples{
# Residual cross-products are accumulated only for feature pairs that
# coexist in some searchlight, so the cost tracks support topology rather
# than the square of the feature count.
example <- example_fmri_effects()
statistics <- crossform:::residual_pair_statistics(
  example$fit, example$frame
)
length(statistics$pair_i)
statistics$partitions

# One atomic record per partition, each carrying its own residual df.
statistics$atomic[["run1"]]$residual_df

# The workspace budget is a cache size, not part of the numerical shape,
# so shrinking it cannot change the accumulated values.
frugal <- crossform:::residual_pair_statistics(
  example$fit, example$frame, workspace_bytes = 4 * 1024^2
)
identical(frugal$signature, statistics$signature)
}
\seealso{
\code{\link[=plan_crossnobis]{plan_crossnobis()}}, which compiles these statistics into a
learned metric schedule, and \code{\link[=rdm_sampling_covariance]{rdm_sampling_covariance()}}, which can reuse
them through \code{residual_strategy = "shared_pair_statistics"}.
}
\keyword{internal}
