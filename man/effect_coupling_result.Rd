% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling-views.R
\name{effect_coupling_result}
\alias{effect_coupling_result}
\title{Coupling results and the readings they may carry}
\description{
Every view over a completed \code{effect_measurement_form} returns one
\code{effect_coupling_result}. There are no subclasses: the seven readings
below are distinguished by the \verb{$kind} string, which is a \strong{closed}
enumeration. A result of any kind carries the same fourteen fields, is
validated the same way, and prints the same way, so code that reads one
kind reads them all; only the shape of \verb{$values} differs, and it differs
two ways rather than seven.
}
\details{
Every kind carries \verb{$values}, \verb{$edge_index} naming the edges those values
are keyed to, \verb{$source_plan} and \verb{$source_receipt} identifying the forms it
was read from, \verb{$normalization_axis} and \verb{$summary_axis} stating what was
divided out and what was summarized over, \verb{$stage_order} listing the
pipeline that produced it, \verb{$units} and \verb{$terminology} stating what the
numbers may be called, \verb{$edge_completeness}, and a \verb{$signature} over all of
it. \verb{$regularization} and \verb{$partition_policy} are present exactly when the
kind applied one; they are \code{NULL} otherwise, never silently defaulted.
}
\section{Kinds}{

\verb{$kind} is one of the following seven values and no others. Constructing a
result with an unlisted kind, or with a \verb{$values} shape that disagrees with
its kind, is a contract error rather than an accepted record.

Two kinds report \strong{blocks}: \verb{$values} is a named list holding one numeric
matrix per edge, in \verb{$edge_index$edge_id} order.
\itemize{
\item \code{"effect_coupling"} -- the raw measurement block, with no
covariance claim attached. \verb{$normalization_axis} is \code{"none"} and
\verb{$units} is \code{NULL}, because nothing has been divided out and nothing is
claimed about what the numbers mean.
\item \code{"covariance_coupling"} -- the same blocks, now certified as
repeated-sample covariance: the form established repeated variation of
effective rank above one and symmetric positive self-blocks.
}

Five kinds report a \strong{table}: \verb{$values} is a data frame keyed by \code{edge_id}
whose remaining columns are fixed by the kind.
\itemize{
\item \code{"pearson_correlation"} -- columns \code{edge_id}, \code{correlation}. One
signed scalar per edge, requiring rank-one measurement axes.
\item \code{"partitioned_pearson_coupling"} -- columns \code{edge_id}, \code{value},
\code{transform}. A weighted reduction across several source forms; carries
a \verb{$partition_policy} recording the weights, the placement, and the
edge transform, and \verb{$units} is \code{"correlation"} or \code{"fisher_z"}
according to that transform.
\item \code{"canonical_coupling"} -- columns \code{edge_id}, \code{mode},
\code{canonical_correlation}. Several rows per edge, one per canonical mode
in descending order; carries the \verb{$regularization} whose ridge changed
the values.
\item \code{"geometry_alignment"} -- columns \code{edge_id}, \code{geometry_alignment}.
Static linear CKA/RV-like alignment, normalized over form entries
rather than over experimental samples.
\item \code{"gaussian_mutual_information"} -- columns \code{edge_id},
\code{information}, \code{units}. Carries the \verb{$regularization} used for the
underlying canonical spectrum, and a \verb{$terminology} naming the
signature of the \code{\link[=gaussian_covariance_model]{gaussian_covariance_model()}} declaration it rests on.
}
}

\seealso{
\code{\link[=effect_coupling]{effect_coupling()}}, \code{\link[=covariance_coupling]{covariance_coupling()}},
\code{\link[=canonical_coupling]{canonical_coupling()}}, \code{\link[=geometry_alignment]{geometry_alignment()}}, and \code{\link[=connectivity]{connectivity()}},
which produce these results, and \code{\link[=gaussian_covariance_model]{gaussian_covariance_model()}} for the
declaration Gaussian information requires.

Other coupling and connectivity views: 
\code{\link{connectivity}()},
\code{\link{control_coupling}()},
\code{\link{coupling}()},
\code{\link{coupling_contrast}()},
\code{\link{coupling_views}},
\code{\link{match_control}()},
\code{\link{match_coupling}()},
\code{\link{measurement_components}()},
\code{\link{measurement_form}()},
\code{\link{pair_lm_query}()},
\code{\link{pair_query}()}
}
\concept{coupling and connectivity views}
