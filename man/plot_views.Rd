% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-methods.R
\name{plot_views}
\alias{plot_views}
\alias{plot.effect_contrast_view}
\alias{plot.effect_rdm_view}
\alias{plot.effect_rsa_view}
\alias{plot.effect_crossnobis_view}
\alias{plot.effect_sampling_covariance}
\title{Plot geometry views}
\usage{
\method{plot}{effect_contrast_view}(
  x,
  which = c("both", "decomposition", "profile"),
  highlight = NULL,
  highlight_label = "highlighted",
  main = NULL,
  top = 200L,
  ...
)

\method{plot}{effect_rdm_view}(x, measurement = NULL, annotate = NULL, main = NULL, ...)

\method{plot}{effect_rsa_view}(
  x,
  terms = NULL,
  highlight = NULL,
  highlight_label = "highlighted",
  main = NULL,
  top = 200L,
  ...
)

\method{plot}{effect_crossnobis_view}(
  x,
  highlight = NULL,
  highlight_label = "highlighted",
  main = NULL,
  top = 200L,
  ...
)

\method{plot}{effect_sampling_covariance}(x, estimate = NULL, level = 0.95, sort = TRUE, main = NULL, ...)
}
\arguments{
\item{x}{A view object: \code{effect_contrast_view}, \code{effect_rdm_view},
\code{effect_rsa_view}, \code{effect_crossnobis_view}, or
\code{effect_sampling_covariance}.}

\item{which}{Which contrast panels to draw: \code{"both"} (the default
two-panel figure), \code{"decomposition"} for the coherent/configuration
plane alone, or \code{"profile"} for the total-energy index plot alone.}

\item{highlight}{Optional measurements to mark: positions, a logical mask
with one value per measurement, or measurement identifiers. Measurements
outside the view are an error rather than a silent drop.}

\item{highlight_label}{What the highlighted measurements are, named in the
legend alongside their count, as in \code{"planted signal (104 measurements)"}.
Say what the set means rather than that it was selected.}

\item{main}{Optional title. For a multi-panel figure a character vector is
recycled across panels.}

\item{top}{How many measurements keep a needle of their own once a profile
panel holds more than five thousand of them, taken as the \code{top}
measurements furthest from the reference line in either direction. The
default is 200, capped at the number of measurements the view has. The
argument is validated on every call, including views small enough to
draw every measurement, where it is otherwise unused.}

\item{...}{Further arguments passed to the underlying base-graphics call
for each panel, such as \code{pch}, \code{cex}, \code{xlim}, or \code{col}.}

\item{measurement}{Which measurement to draw. For an \code{effect_rdm_view},
one position or identifier, or the string \code{"mean"} for the mean
dissimilarity matrix over measurements. The default is the measurement
with the largest mean distance.}

\item{annotate}{Whether to print the numeric distance inside each cell of
an RDM heatmap. The default prints them when there are at most eight
experimental effects.}

\item{terms}{Optional character vector selecting which RSA coefficient
columns to draw. The default draws every column, including the
intercept and any nuisance terms.}

\item{estimate}{Optional point estimates to center the sampling intervals
on: a numeric vector over the distance labels carried by \code{x}, or an
\code{effect_rdm_view}, from which the row for the measurement \code{x} describes
is taken. The default centers the intervals on zero, which shows the
interval half-width implied by the declared calibration target.}

\item{level}{Two-sided normal coverage for the sampling interval. This is
a width computed from the analytic sampling covariance, not a
calibrated confidence interval or a group-level test.}

\item{sort}{Whether to order the distance rows by their plotted center.}
}
\value{
The view \code{x}, invisibly. Called for the picture it draws.
}
\description{
Base-graphics pictures of the objects returned by \code{\link[=contrast_energy]{contrast_energy()}},
\code{\link[=rdm]{rdm()}}, \code{\link[=rsa]{rsa()}}, \code{\link[=crossnobis]{crossnobis()}}, and \code{\link[=rdm_sampling_covariance]{rdm_sampling_covariance()}}. Each
method draws only what the view already holds: nothing is recomputed,
nothing is smoothed, and negative crossvalidated estimates are drawn where
they fall rather than clipped at zero.
}
\details{
\code{plot()} on an \code{effect_contrast_view} is the fastest way to see the
central claim of the package. The left panel places every measurement in
the coherent/configuration plane, where the dashed guides are lines of
constant \code{total} because \code{coherent + configuration = total} exactly. The
right panel walks the same total along the measurement index. Passing
\code{highlight} (for the generated fixture,
\code{example_fmri_effects()$truth$signal_measurements}) marks the
measurements that overlap the planted signal, so a correct analysis is
visible rather than asserted.

One color means one thing across both panels. Color is the sign of the
signed marginal, blue above zero and orange-red below; a black ring is
"highlighted", and the fill inside it still carries the sign; plain gray is
"not highlighted". When no \code{highlight} is given there is nothing for gray
to mean, and every measurement keeps its sign color.

Glyph size and opacity ease down as a panel fills, so a view of a few
thousand measurements stays readable; at or below five hundred
measurements nothing is rescaled. Above five thousand measurements one
glyph per measurement is no longer a picture of the data, and the contrast
panels change what they draw rather than overplotting. The profile panel
shades the central 95\% of the values as a band, rules the median across
it, and keeps needles only for the \code{top} measurements furthest from the
reference line in either direction, so the negative half of a
crossvalidated estimate is still shown. The decomposition panel bins the
coherent/configuration plane into rectangular cells whose opacity follows
the log count and whose color is the signed marginal that dominates the
cell; the binning is a plain count, with no kernel and no smoothing.
Highlighted measurements are always drawn individually, on top of either
summary, however many of them there are.
}
\examples{
example <- example_fmri_effects()
plan <- plan_geometry(
  example$fit$relation, example$frame,
  cross_partitions(
    example$fit$relation,
    independence = "independent", generalizes_over = "run"
  )
)
planted <- example$truth$signal_measurements

# Where does the animate-versus-inanimate contrast reproduce, and how does
# its energy split? The planted measurements fill in.
energy <- contrast_energy(plan, example$contrast)
plot(energy, highlight = planted, highlight_label = "planted signal")

# The signed squared distances at the strongest measurement.
distances <- rdm(plan)
plot(distances, measurement = which.max(energy$total))

# One panel per RSA coefficient, over measurements.
plot(rsa(plan, models = list(category = example$model_rdm)),
     highlight = planted, highlight_label = "planted signal")

# The same estimand under a declared fixed noise metric.
metric <- noise_precision(
  diag(example$domain$n_features), example$domain
)
mahalanobis_plan <- plan_geometry(
  example$fit$relation, example$frame,
  cross_partitions(example$fit$relation, independence = "independent"),
  metric = metric
)
plot(crossnobis(mahalanobis_plan, example$contrast), highlight = planted)

# Analytic within-measurement uncertainty for those distances.
uncertainty <- rdm_sampling_covariance(
  plan, example$fit, target = "null", at = which.max(energy$total)
)
plot(uncertainty, estimate = distances)
}
\seealso{
\code{\link[=contrast_energy]{contrast_energy()}}, \code{\link[=rdm]{rdm()}}, \code{\link[=rsa]{rsa()}}, \code{\link[=crossnobis]{crossnobis()}}, and
\code{\link[=rdm_sampling_covariance]{rdm_sampling_covariance()}} for the objects these methods draw, and
\code{\link[=as.data.frame]{as.data.frame()}} methods for the same values as a table.

Other geometry plans and views: 
\code{\link{aggregate_first}()},
\code{\link{bilinear_query}()},
\code{\link{compute_policy}()},
\code{\link{contrast_energy}()},
\code{\link{contribution}()},
\code{\link{crossnobis}()},
\code{\link{evaluate_geometry}()},
\code{\link{example_fmri_effects}()},
\code{\link{geometry_component}()},
\code{\link{geometry_spectrum}()},
\code{\link{materialize_geometry}()},
\code{\link{plan_crossnobis}()},
\code{\link{plan_geometry}()},
\code{\link{query_geometry}()},
\code{\link{rdm}()},
\code{\link{rsa}()},
\code{\link{variation_query}()}
}
\concept{geometry plans and views}
