% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame.R
\name{frame_family}
\alias{frame_family}
\title{Combine conservative frames into one alpha-weighted family}
\usage{
frame_family(
  ...,
  alpha = NULL,
  normalization = "conservative",
  tolerance = 1e-12
)
}
\arguments{
\item{...}{Two or more compiled \code{effect_frame}s over one domain, ideally
named. A member's name becomes its \code{family} identity; unnamed members are
named \code{frame1}, \code{frame2}, and so on by position. Pass a list of frames
with \code{do.call(frame_family, c(frames, list(alpha = alpha)))}.}

\item{alpha}{One positive family weight per member, summing to one. Named
weights are matched to member names; \code{NULL} (the default) weights the
members equally. Weights are never renormalized for you, because the
per-block law reads against the weight actually applied.}

\item{normalization}{Frame normalization of the family. Only
\code{"conservative"} is defined: the per-block law needs column-normalized
members, and a family of locally normalized frames has no conserved
budget to weight.}

\item{tolerance}{Nonnegative absolute tolerance for the two construction
checks: that \code{alpha} sums to one, and that each member's columns sum to
one on its own.}
}
\value{
An \code{effect_frame} usable anywhere a compiled frame is, carrying
\verb{$weights} (the alpha-scaled row-bind), \verb{$index} (one row per
measurement, see \emph{Per-row metadata}), and a \verb{$specification} recording
every member specification together with its applied weight.
}
\description{
Stacks several conservative frames over one domain into a single compiled
frame whose rows are the members' rows scaled by family weights \code{alpha}.
Because each member is column-normalized on its own and the weights sum to
one, the stacked columns still sum to one, so the family is conservative and
the \code{total} component conserves both overall and block by block:
\deqn{\sum_{x \in s} G_{s,x} = \alpha_s\,G_\Omega,\qquad
      \sum_x G_x = G_\Omega.}{sum_{x in s} G_{s,x} = alpha_s G_Omega, and sum_x G_x = G_Omega.}
}
\details{
The consequence is that per-scale \emph{energy} is fixed by \code{alpha} alone and is
never a finding. What varies with the data is the split of each block's
fixed budget into coherent and configuration parts. See
\code{design/conservative-geometry-contract.md} section 3.
}
\section{Per-row metadata}{

A family row must be self-describing, because its own scale and provenance
can no longer be read off a frame-wide specification. \verb{$index} therefore
carries one row per measurement, in \verb{$weights} row order:
\itemize{
\item \code{measurement}: the row's identity, \code{"<family>::<node>"}. It is unique
across the family, which the same node label appearing at several scales
is not, and it is what reaches a result's \verb{$index}.
\item \code{family}: the member the row came from.
\item \code{node}: the row's label inside its own member, exactly as that member's
\verb{$index$measurement} had it.
\item \code{scale}: the member's scale parameter -- the radius for a searchlight
member, \code{NA} for a member that has no scale.
\item \code{center}: the anchor feature identifier, for members whose rows are
anchored at a feature (points and searchlights); \code{NA} otherwise.
\item \code{alpha}: the family weight applied to the row.
}

Join a result back to this table by \code{measurement} to group values by scale,
by center, or by member.
}

\examples{
domain <- abstract_domain(
  9L, coordinates = cbind(seq_len(9L) - 1, 0), id = "frame-family-example"
)

# Two conservative scales over one domain, weighted one quarter and three
# quarters.
family <- frame_family(
  point = compile_frame(voxelwise("conservative"), domain),
  narrow = compile_frame(searchlights(1.01, "conservative"), domain),
  alpha = c(point = 0.25, narrow = 0.75)
)
head(family$index, 3L)

# The stacked family conserves, and each block carries exactly its alpha.
report <- frame_conservation(family)
report$conserved
report$members

# Weights that do not sum to one are refused rather than renormalized: the
# per-block law reads against the weight actually applied.
refused <- try(
  frame_family(
    point = compile_frame(voxelwise("conservative"), domain),
    narrow = compile_frame(searchlights(1.01, "conservative"), domain),
    alpha = c(1, 1)
  ),
  silent = TRUE
)
conditionMessage(attr(refused, "condition"))
}
\seealso{
\code{\link[=compile_frame]{compile_frame()}} for the members, \code{\link[=frame_conservation]{frame_conservation()}}, which
certifies a family both overall and block by block, and
\code{\link[=searchlights]{searchlights()}}, whose multiscale form is the shorthand for a family of
conservative searchlight frames at several radii.

Other neural domains and frames: 
\code{\link{abstract_domain}()},
\code{\link{additive_frame}()},
\code{\link{as_neurovol}()},
\code{\link{compile_frame}()},
\code{\link{edge_frame}()},
\code{\link{frame_conservation}()},
\code{\link{measurement_frame}()},
\code{\link{neuroim2_searchlights}()},
\code{\link{neuroim2_volume_domain}()},
\code{\link{regions}()},
\code{\link{searchlights}()},
\code{\link{volume_domain}()},
\code{\link{voxelwise}()},
\code{\link{whole_brain}()}
}
\concept{neural domains and frames}
