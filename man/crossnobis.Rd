% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossnobis-driver.R
\name{crossnobis}
\alias{crossnobis}
\title{Evaluate a signed local crossnobis contrast}
\usage{
crossnobis(x, weights)
}
\arguments{
\item{x}{An \code{effect_geometry_plan} carrying either an explicit fixed
\code{noise_precision()} metric, or a provenance-frozen learned metric
schedule compiled from a recipe by \code{\link[=plan_crossnobis]{plan_crossnobis()}} or
\code{plan_geometry(metric = )}.}

\item{weights}{One finite contrast weight per experimental effect.}
}
\value{
An \code{effect_crossnobis_view} whose \verb{$values} holds one signed
crossvalidated squared Mahalanobis value per spatial measurement, with
the aligned \verb{$contrast}, the named \verb{$estimand}, the \verb{$metric} and
\verb{$pairing} identities, \verb{$index}, and the executed \verb{$receipt}.
}
\description{
The result is the query-first evidence
\verb{tr(c c^T B_a K B_b^T)} aggregated over the plan's independent partition
edges. This function is a validating view over the ordinary geometry
compiler; it does not introduce a second numerical engine. Negative finite
estimates are retained.
}
\section{Structure}{

One signed value per spatial measurement, alongside the declarations that
make it a Mahalanobis reading.
\itemize{
\item \verb{$values}: the signed crossvalidated squared Mahalanobis contrast, one
per measurement, in \verb{$index} order. Negative estimates are retained.
\item \verb{$contrast}: the weights, reordered to the relation's effect order and
named.
\item \verb{$estimand}: the named estimand,
\code{"crossvalidated_squared_mahalanobis_contrast"}.
\item \verb{$metric}: the signature of the noise-precision metric the values were
read under.
\item \verb{$pairing}: the identity of the independent partition edges they were
generalized over.
\item \verb{$index}: the measurement identifiers, one per value, carried from the
frame's \verb{$index$measurement}.
\item \verb{$receipt}: the execution receipt for the run that produced the values.
}

The \verb{$metadata} block and any other element not listed here are internal
and may change.
}

\section{Refusal}{

A plan carrying the implicit identity metric, or a fixed metric that was not
built by \code{\link[=noise_precision]{noise_precision()}}, signals an \code{effect_capability_refusal} with
capability \code{"declared_noise_metric"} in namespace \code{"geometry_views"}.
Inspect it with \code{\link[=catch_refusal]{catch_refusal()}}.
}

\section{One estimand, two views}{

On a fixed-metric geometry plan, \code{crossnobis(x, weights)} is the named
Mahalanobis reading of exactly \code{contrast_energy(x, weights)$total}: the same
compiled estimand, exposed as a single signed value. When the analysis
also needs the signed endpoint marginals or the exact
coherent/configuration decomposition of the same quantity, call
\code{\link[=contrast_energy]{contrast_energy()}} on the same plan; every component it returns inherits the
plan's fixed metric.
}

\examples{
domain <- abstract_domain(3, id = "crossnobis-example")
run1 <- rbind(a = c(1, 0, 0), b = c(0, 1, 0))
run2 <- rbind(a = c(1.1, 0, 0), b = c(0, 0.9, 0))
relation <- relation(list(run1 = run1, run2 = run2), domain = domain)
plan <- plan_geometry(
  relation,
  compile_frame(whole_brain(), domain),
  cross_partitions(relation, independence = "independent"),
  metric = noise_precision(diag(3), domain)
)
result <- crossnobis(plan, c(a = 1, b = -1))
result
as.data.frame(result)
}
\seealso{
\code{\link[=noise_precision]{noise_precision()}} for the fixed metric a geometry plan needs,
\code{\link[=plan_crossnobis]{plan_crossnobis()}} for the learned-metric route, and
\code{\link[=contrast_energy]{contrast_energy()}} for the decomposed reading of the same estimand.

Other geometry plans and views: 
\code{\link{aggregate_first}()},
\code{\link{bilinear_query}()},
\code{\link{compute_policy}()},
\code{\link{contrast_energy}()},
\code{\link{evaluate_geometry}()},
\code{\link{example_fmri_effects}()},
\code{\link{geometry_component}()},
\code{\link{geometry_spectrum}()},
\code{\link{materialize_geometry}()},
\code{\link{plan_crossnobis}()},
\code{\link{plan_geometry}()},
\code{\link{plot_views}},
\code{\link{query_geometry}()},
\code{\link{rdm}()},
\code{\link{rsa}()},
\code{\link{variation_query}()}
}
\concept{geometry plans and views}
