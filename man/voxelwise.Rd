% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame.R
\name{voxelwise}
\alias{voxelwise}
\title{Specify point measurements}
\usage{
voxelwise(normalization = "conservative")
}
\arguments{
\item{normalization}{Explicit frame normalization.}
}
\value{
An \code{effect_frame_spec} with \verb{$kind} \code{"voxels"} and the requested
\verb{$normalization}. Pass it to \code{\link[=compile_frame]{compile_frame()}} to obtain measurements.
}
\description{
Declares one measurement per neural feature. This is the finest scope and,
under the default conservative normalization, the only spatial scope whose
local values sum exactly to the whole-brain value.
}
\examples{
domain <- abstract_domain(4L, id = "voxelwise-example")

# One measurement per feature: the operator is the identity.
frame <- compile_frame(voxelwise(), domain)
dim(frame$weights)

# Conservative normalization is the default here, so local totals add up to
# the whole-brain total exactly.
frame_conservation(frame)$conserved
}
\seealso{
\code{\link[=compile_frame]{compile_frame()}}, and \code{\link[=searchlights]{searchlights()}}, \code{\link[=regions]{regions()}}, or
\code{\link[=whole_brain]{whole_brain()}} for coarser scopes.

Other neural domains and frames: 
\code{\link{abstract_domain}()},
\code{\link{additive_frame}()},
\code{\link{as_neurovol}()},
\code{\link{compile_frame}()},
\code{\link{edge_frame}()},
\code{\link{frame_conservation}()},
\code{\link{frame_family}()},
\code{\link{measurement_frame}()},
\code{\link{neuroim2_searchlights}()},
\code{\link{neuroim2_volume_domain}()},
\code{\link{regions}()},
\code{\link{searchlights}()},
\code{\link{volume_domain}()},
\code{\link{whole_brain}()}
}
\concept{neural domains and frames}
