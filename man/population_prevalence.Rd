% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population-prevalence.R
\name{population_prevalence}
\alias{population_prevalence}
\title{Count how many participants a transported group value stands on}
\usage{
population_prevalence(
  x,
  query = NULL,
  threshold = 0,
  coverage_floor = NULL,
  term = NULL
)
}
\arguments{
\item{x}{An \code{effect_population_result} from \code{\link[=estimate_population]{estimate_population()}}.}

\item{query}{Which queries to count over, by name or position. \code{NULL} (the
default) counts over every query in the bank. The alignment inner product
is taken over exactly the selected coordinates.}

\item{threshold}{The value a participant's transported ledger must strictly
exceed to be counted, in the ledger's own units. \code{0} is the default and
counts participants with a positive contribution.}

\item{coverage_floor}{Optional whole number. Group nodes whose smallest
contributing-participant count falls below it are listed in
\verb{$coverage$below_floor}. \code{NULL} (the default) reports the counts and marks
nothing.}

\item{term}{Refused. Present so that a caller arriving from
\code{\link[=population_uncertainty]{population_uncertainty()}} gets a reason rather than silence.}
}
\value{
An \code{effect_population_prevalence}: \verb{$sign} holding \verb{$fraction},
\verb{$count}, \verb{$resolved} and \verb{$contributing} (each a \code{node}-by-\code{query}
matrix); \verb{$alignment} holding \verb{$fraction}, \verb{$count}, \verb{$resolved} and the
inner product it was taken in, one entry per group node; \verb{$coverage};
\verb{$reference}, the fraction a pure-noise cell reports; \verb{$layer} and
\verb{$reading}, which mark the record as descriptive; \verb{$queries} and
\verb{$query_labels}, the selected rows of the bank it counted over; and the
\verb{$index}, \verb{$ledger}, \verb{$semantics}, \verb{$normalization} and \verb{$receipt} of the
result it read. \code{as.data.frame(x, measure = )} returns one measure in long
form.
}
\description{
\code{population_prevalence()} reports two fractions over the participants of an
\code{\link[=estimate_population]{estimate_population()}} result. \strong{Sign prevalence} is, at each group node
and query, the fraction of participants whose transported ledger value
exceeds \code{threshold}. \strong{Alignment prevalence} is, at each group node, the
fraction of participants whose whole profile across the query bank points
the same way as the rest of the group's.
}
\details{
Both are descriptive, both live on the latent layer of
\code{design/conservative-geometry-contract.md} section 6 as
\code{design/population-form-contract.md} section 6.5 applies it, and neither
carries or admits an error bar. The record says so in \verb{$layer}, in
\verb{$reading}, and on every printed line.
}
\section{Why this is a latent-layer record}{

The layer projects nothing --- there is no eigenvalue truncation here and no
moved mass to account for --- but \code{value > threshold} is the same kind of
operation and fails in the same direction. It is a per-participant sign
clamp: it discards the magnitude of a crossvalidated estimate and keeps the
sign, which is the part that carries the noise. \strong{A group node and query at
which nothing reproduces reports a fraction near \code{0.5}, not near \code{0}},
because every participant contributes an independent coin flip. \verb{$reference}
carries that number so the comparison is not made against zero by habit.

The threshold is applied strictly (\code{>}) and absolutely, in the ledger's own
units, with no relative tolerance. That is the guard every per-node fraction
in the package uses, and whether these guards should take a relative
tolerance is an open contract decision
(\code{conservative-geometry-contract.md} section 11.4, gap G3) that this layer
inherits rather than settling privately. A cell that is zero only to within
rounding is counted by its rounding.
}

\section{There is no inference here, and none is derivable}{

A count of participants is not an estimate of a population quantity on this
layer. The participants are not a sample from a declared superpopulation of
transports, and the binomial variance of a thresholded crossvalidated
estimate is not the sampling variance of anything the plan sealed. This
function computes no standard error, no interval and no p-value, the record
refuses to carry a field named like one, and constructing a prevalence
leaves the result's own \verb{$uncertainty} untouched.
\code{\link[=population_uncertainty]{population_uncertainty()}} is the separate inferential layer, and it carries
its own measured calibration record.
}

\section{What alignment is an inner product in}{

Participant \code{i}'s profile at a group node is \verb{v_i = H y_i}, with \code{H} the
packed queries the driver lowers the bank to and \code{y_i} the participant's
packed transported form. The scored quantity is
\eqn{\langle v_i, \bar v_{-i}\rangle}{<v_i, mean of the others>} against the
\strong{leave-one-out} mean of the other participants. The reference excludes the
participant being scored on purpose: the plain group mean puts the positive
self term \eqn{\lVert v_i\rVert^2/n}{||v_i||^2 / n} inside every product, so
the fraction would be inflated by construction and would read \code{1} at
\code{n = 1}. Excluding it makes every product a cross-participant one, which is
the same device \code{population-form-v1} sections 6.2 and 7.1 use.

That inner product is the Frobenius inner product of the transported forms
\emph{restricted to the bank's span} exactly when the packed queries are
orthonormal, and a Euclidean inner product in coordinates the bank chose
otherwise. \verb{$alignment$readout_gram_deviation} measures the departure and
\verb{$alignment$frobenius_equivalent} states the verdict; a two-condition
contrast bank is not orthonormal.
}

\section{The sink, and coverage}{

The sink is excluded, on the same grounds \code{\link[=heterogeneity]{heterogeneity()}} excludes it: it
is unmapped territory reported in budget units at no location, so "the
fraction of participants whose unmapped mass is positive" is a statement
about transport failure and not about a place. The exclusion is recorded at
\verb{$receipt$prevalence$sink_excluded} and the per-participant sink budget
stays readable at \verb{$receipt$sink_budget}.

A prevalence is uninterpretable without the number of participants behind
it. \verb{$coverage$contributing} is the result's exact node-query availability,
derived from realized transported mass and query admission rather than from
whether the observed value is nonzero. \code{coverage_floor} marks the nodes
below a declared floor; it has no default because section 14.3 records the
threshold itself as an open maintainer decision.
}

\section{Refusals}{

Each is an \code{effect_capability_refusal} in namespace
\code{"population_prevalence"} (see \code{\link[=catch_refusal]{catch_refusal()}}).
\itemize{
\item \code{population_participant_values} --- a \code{materialize_population()} result.
The streamed complete-form route retains no participant axis, by design.
\item \code{participant_term_decomposition} --- a non-\code{NULL} \code{term}. Participants
carry one transported ledger, not one per group-model column.
\item \code{comparable_ledger_orientation} --- a \code{"unit_budget"} population in which
some participant's signed native total is negative, so its values are
sign-flipped relative to the others and a sign count would mix two
questions.
}

One refusal is carried rather than raised. The form-space reading --- the
fraction of participants whose transported \emph{form} has positive Frobenius
inner product with the group mean form --- needs per-participant transported
forms, and no population result ships them: the query-bank route retains
each participant's value only in the coordinates the declared bank names,
and the streamed route retains no participant axis. Capability
\code{participant_form_prevalence} travels on
\verb{$receipt$form_prevalence_refusal} with its reasons and remedies.
}

\examples{
# Six participants on different native frames, carrying one planted effect
# every run of every participant shares (face over house) and a phase-shifted
# pattern that reproduces in none of them. Two group nodes: one every
# participant reaches and one only the larger native frames do.
effects <- effect_space(c("face", "house", "tool"), basis_id = "popprev:v1")
subject <- function(id, n, phase) {
  domain <- abstract_domain(n, coordinates = cbind(x = seq_len(n) - 1),
    feature_ids = paste0("f", seq_len(n)), id = id)
  planted <- outer(c(face = 0.8, house = -0.8, tool = 0), rep(1, n))
  values <- function(shift) matrix(cos(seq_len(3 * n) + shift), 3, n,
    dimnames = list(c("face", "house", "tool"), NULL)) + planted
  rel <- relation(list(run1 = values(0), run2 = values(phase)),
    effects = effects, domain = domain)
  plan_geometry(rel, compile_frame(voxelwise(), domain),
    cross_partitions(rel))
}
carrier <- function(n) anatomical_transport(
  native_coords = cbind(seq_len(n) - 1), group_coords = cbind(c(0, 6)),
  semantics = "budget", radius = 2
)
sizes <- c(s01 = 4L, s02 = 5L, s03 = 6L, s04 = 7L, s05 = 8L, s06 = 9L)
phases <- c(s01 = 0.2, s02 = 1.1, s03 = 2.4, s04 = 0.6, s05 = 3, s06 = 1.7)
subjects <- stats::setNames(lapply(names(sizes), function(id)
  subject(id, sizes[[id]], phases[[id]])), names(sizes))
plan <- plan_population(subjects, lapply(sizes, carrier))
fit <- estimate_population(plan,
  rbind(`face-house` = c(1, -1, 0), `face-tool` = c(1, 0, -1)))

shared <- population_prevalence(fit)
shared

# The planted contrast is carried by every participant; the one that
# reproduces in none of them sits at the 0.5 the print warns about.
shared$sign$fraction

# And how many participants were there to be counted at each node.
shared$coverage$contributing

# A term axis does not exist on participant values, and asking says why.
catch_refusal(population_prevalence(fit, term = "(Intercept)"))$capability
}
\references{
\code{design/population-form-contract.md} (\code{population-form-v1}),
sections 6.5, 7.5 and 8.1;
\code{design/conservative-geometry-contract.md} (\code{conservative-geometry-v1}),
section 6.
}
\seealso{
\code{\link[=latent_geometry]{latent_geometry()}} for the projection layer this one shares its
discipline with, \code{\link[=population_uncertainty]{population_uncertainty()}} for the separate inferential
layer, and \code{\link[=heterogeneity]{heterogeneity()}} for the form-space reading of how
participants differ.

Other population transports: 
\code{\link{anatomical_transport}()},
\code{\link{estimate_population}()},
\code{\link{external_transport}()},
\code{\link{heterogeneity}()},
\code{\link{location_transport}()},
\code{\link{materialize_population}()},
\code{\link{plan_population}()},
\code{\link{population_uncertainty}()},
\code{\link{population_views}},
\code{\link{population_wild_bootstrap}()},
\code{\link{transport_values}()}
}
\concept{population transports}
