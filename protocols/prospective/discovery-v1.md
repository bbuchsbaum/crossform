# Frozen prospective discovery specification

Status: BLOCKED until an eligible dataset manifest is selected

Evidence state: prospective protocol, not an empirical result

Frozen: 2026-08-21

The canonical executable configuration is discovery-v1.json. Its null dataset
identity, version, and manifest hash are blockers, not degrees of freedom and
not fabricated result values. They may be filled only after eligibility-v1 is
applied without inspecting the desired outcomes.

Condition roles and both contrasts, all unordered independent partition pairs,
the whole eligible declared support, conservative radial scales 0/4/8/12 mm,
fixed anatomical primary transport, leave-one-subject-out functional secondary
transport, intercept population target, all-planned coverage, no normalization,
HC3 pointwise primary uncertainty, and 1,999-draw wild-bootstrap sensitivity
are frozen.

Primary outcomes are comparator ambiguity and component-profile interpretive
gain under comparators-v1. Population component coefficients and transport
comparison are secondary. Coverage, effective N, sink, transport quality,
influence, and negative controls are diagnostic. The configuration enumerates
every required table, figure, manifest, deviation log, and typed failure state.

Exploratory analyses must write only below exploratory/, use a separate
manifest, and are never headline eligible. A post-access change cannot overwrite
this file or JSON. It creates a new version in amendments/ with date, author,
reason, affected fields, access state, and consequences for evidence status.
